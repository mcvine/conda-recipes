package:
  name: histogram
  version: "0.3.3"

source:
  git_rev: v0.3.3
  git_url: https://github.com/danse-inelastic/histogram.git

requirements:
  build:
    - python
    - numpy           {{ numpy }}
    - journal
    - cmake

  run:
    - python
    - numpy
    - danse.ins
    - journal
    - pyre
    - h5py

build:
  script_env:
    - CC
    - CXX

test:
  imports:
    - histogram
  

about:
  home: 
  license:
  license_file:
